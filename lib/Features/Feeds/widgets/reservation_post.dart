import 'package:flutter/cupertino.dart';

class ReservationPost extends StatelessWidget {
  const ReservationPost({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}